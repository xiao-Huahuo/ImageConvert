def jpg_to_svg(input_path, output_path):
    return "不支持的转换：无法将光栅图像 (JPG) 转换为矢量图像 (SVG)。"

def svg_to_jpg(input_path, output_path):
    return "不支持的转换：SVG 渲染需要专门的库 (如 'cairosvg')。"

def jpeg_to_svg(input_path, output_path):
    return "不支持的转换：无法将光栅图像 (JPEG) 转换为矢量图像 (SVG)。"

def svg_to_jpeg(input_path, output_path):
    return "不支持的转换：SVG 渲染需要专门的库 (如 'cairosvg')。"
